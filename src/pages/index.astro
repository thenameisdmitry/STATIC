---
import BaseLayout from '../layouts/BaseLayout.astro';
import Redact from '../components/Redact.astro';

const channels = [
  { href: '/writer', ch: '01', label: 'DZ: The Writer', blurb: 'Who I am, the career change, and why narrative.', featured: false, img: '/home/the-writer.png' },
  { href: '/game', ch: '02', label: 'STATIC: The Game', blurb: 'A vertical slice — lore, playable dialogue, case files, screenplay.', featured: true, img: '/home/the-game.png' },
  { href: '/book', ch: '03', label: 'MYST: The Book', blurb: 'Excerpts, illustrations, and a screen adaptation.', featured: false, img: '/home/the-book.png' },
  { href: '/lab', ch: '04', label: 'MY LAB: Ideas', blurb: 'Short experiments, barks, and micro-fiction.', featured: false, img: '/home/the-lab.png' },
  { href: '/contact', ch: '05', label: 'Intercept My Signal', blurb: 'CV, email, and where to reach me.', featured: false, img: '/home/the-contact.png' },
];
---

<BaseLayout title="Dmitrii Zhukov — Writer">
  <!-- HERO -->
  <section class="hero-frame relative px-6 pt-[0vh] pb-[0vh]"></section> 
  <section class="relative px-6 pt-[11vh] pb-[11vh]">
    <div class="fog" aria-hidden="true">
      <div class="fog__layer fog__layer--a"></div>
      <div class="fog__layer fog__layer--b"></div>
      <div class="fog__layer fog__layer--c"></div>
    </div>
    <div class="relative z-10 mx-auto grid w-full max-w-6xl items-center gap-10 lg:grid-cols-[1fr_minmax(0,35rem)] lg:gap-10">        
      <!-- left: title -->
        <div>
          <p class="eyebrow mb-5">◉ Now broadcasting</p>
          <h1
            id="hero-title"
            class="signal-in font-display text-[19vw] font-semibold uppercase leading-[0.82] tracking-tight sm:text-[clamp(4rem,12vw,12rem)]"
          >
            STATIC
          </h1>
          <p class="mt-7 max-w-xl text-lg text-ink-dim">
            <span class="note_home_page">Videogame concept created by Dmitrii Zhukov and 
              based on <Redact label="still in progress">the original novel <i>"Myst"</i>, </Redact> combining psychological thriller and survival 
              horror genres with immersive investigation mechanics, stealth gameplay, and 
              over-the-shoulder hand-to-hand combat.</span>
          </p>
          <a href="#channels" class="group mt-8 inline-flex items-center gap-2 font-mono text-xs uppercase tracking-widest text-ink-dim transition-colors hover:text-violet-bright">
          <span class="transition-transform group-hover:translate-y-0.5">↓</span> Tune in
        </a>
        </div>

        <!-- right: looping trailer (see ASSETS.md) -->
        <div class="trailer">
          <video class="trailer__video" autoplay muted loop playsinline preload="metadata" poster="/media/trailer-poster.jpg">
            <source src="/media/trailer.mp4" type="video/mp4" />
            <source src="/media/trailer.webm" type="video/webm" />
          </video>
          <div class="trailer__placeholder">
            <span class="font-mono text-xs uppercase tracking-widest text-violet-bright">◉ Trailer</span>
            <span class="font-mono text-[0.7rem] leading-relaxed">drop <b>trailer.mp4</b> into<br />/public/media/</span>
          </div>
        </div>
      </div>
    </section>

<!-- INTRO STRIP -->
  <section class="px-6 mt-[15px]">
    <div class="mx-auto max-w-4xl border-y border-line py-12">
      <p class="font-mono text-[0.95rem] leading-relaxed text-ink-dim text-center">

        I design worlds, lore, and narrative experiences that blend mystery, emotion, 
        and player agency. From memorable characters and branching storylines to detective 
        plots and carefully crafted twists, I build stories that reward curiosity and 
        culminate in meaningful catharsis. My game concept <span class="text-violet-bright">Static</span> adapts the world 
        and story of my<Redact label="still in progress">original novel <i>Myst</i>.</Redact>
      </p>
      
    </div>
  </section>

 
  <!-- CHANNEL INDEX -->
  <section id="channels" class="relative scroll-mt-24 px-6 py-20">
    {/* 
    <div class="fog fog--soft" aria-hidden="true"> //uncomment to add og behing the channel cards
      <div class="fog__layer fog__layer--a"></div>
      <div class="fog__layer fog__layer--b"></div>
      <div class="fog__layer fog__layer--c"></div>
    </div>
    */}
    <div class="relative z-10 mx-auto max-w-6xl">
      <p class="eyebrow mb-8">Channel guide</p>
      <div class="grid gap-4 md:grid-cols-2">
        {
          channels.map((c) => (
            <a href={c.href} class={`channel channel--bw ${c.featured ? 'channel--featured md:col-span-2' : ''} p-6 md:p-8`} style={c.img ? `--card-img: url(${c.img})` : undefined}>
              <div class="flex items-baseline justify-between">
                <span class="font-mono text-xs uppercase tracking-widest text-violet">CH {c.ch}</span>
                {c.featured && (
                  <span class="font-mono text-[0.65rem] uppercase tracking-widest text-magenta">◉ Featured project</span>
                )}
              </div>
              <h2 class="mt-3 font-display text-3xl font-semibold uppercase tracking-tight text-ink md:text-4xl">
                {c.label}
              </h2>
              <p class="mt-2 max-w-md text-ink-dim">{c.blurb}</p>
              <span class="mt-4 inline-block font-mono text-xs uppercase tracking-widest text-violet-bright">Open →</span>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <script>
    // Ragged recurring static burst on the STATIC title.
    const title = document.getElementById('hero-title');
    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (title && !reduce) {
      const burst = () => {
        title.classList.add('glitch-active');
        window.setTimeout(() => title.classList.remove('glitch-active'), 460);
        window.setTimeout(burst, 2800 + Math.random() * 3600); // white noise timeframe
      };
      window.setTimeout(burst, 2600);
    }
  </script>

</BaseLayout>
